var g : integer
type x is integer
var h is 1 + 1
routine main(x: integer, y: boolean) : integer is
    var h is 1 + 1
    return not x
end
routine some(m: someType) : integer is
    var a is -5
    type x is integer
    main()
    x.f[1].r.x := -4.0
    return not(not (x < 8) and x > 7)
end
